###### SERVER-SIDE MOCK

# Same test shape, different mock setup

> **`server-mocks.test.js`**
>
> Code sample to be added later.

The page is still rendered by the real server, but the server receives per-test mock instructions.

<!--
Cue: Show that the structure mirrors the client-side test. Run it, then change an assertion to show failure.
-->
